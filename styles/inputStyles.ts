export const inputStyles = {
    base: "w-full",
    inputWrapper:
      "border-none shadow-none ring-0 focus-within:ring-0 focus-within:border-none",
    innerWrapper: "border-none",
    input:
      "bg-transparent outline-none focus:outline-none focus:ring-0 focus-visible:ring-0",
  };

  export const inputWithLabelStyles = {
    ...inputStyles,
    label:"pb-5"
  }